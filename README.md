# To-Do App

A minimal To-Do List using HTML, CSS, and JavaScript.

Features:
- Add new tasks
- Mark tasks as complete
- Delete tasks
- Persistence via `localStorage`

Run

- Open `index.html` in your browser, or run a local server:

```powershell
# from repository root
python -m http.server 8000
# then open http://localhost:8000/
```

Files

- index.html — markup
- styles.css — styling
- script.js — app logic

Next steps

- Add edit task
- Add categories or due dates
- Add tests or build pipeline
